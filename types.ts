export interface Beer {
    name: string,
    brand: string,
    isPremium: boolean,
    createdAt: Date
}
export interface User{
    username: string
    email: string,
    password: string
}
